#+TITLE: Integrate a mcp server

* Explain the problem as you see it

I would like to be able to search and add information to notes.

* Suggest a solution

For the start to implement the possibility of launching the MCP servers where you can use the scheme

#+BEGIN_SRC julia
struct ApiServices
    mcp::Union{Dict{string,MCPType},Nothing}
    llm::Union{LLMType,Nothing}
    openapi::Union{OpenApiType,Nothing}
end

struct LLMType
    serverName::String
end

struct MCPType
    package::String
    disabled::Union{Bool, Nothing}
    env::Union{Dict{String,String}, Nothing}
end

struct OpenApiType
    schemaURL::String
end
#+END_SRC

And first server, become possibility by default
- [[https://github.com/spences10/mcp-duckduckgo-search][MCP duckduckgo server]]

* Release
  DEADLINE: <2025-04-14 Sun>


