import type { ColNavStateProps } from "./col-nav.d.ts"

import { html } from "lit"
// import { unsafeSVG } from "lit/directives/unsafe-svg.js"

export default function ColNavTemplate(_state: ColNavStateProps) {
    return html`
        <nav>
        </nav>
        `
}
